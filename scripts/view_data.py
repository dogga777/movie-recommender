# Import the pandas library to handle our data
import pandas as pd

# Define the file path to our movie dataset
file_path = "data/movies_5000.csv"

# Load the dataset into a DataFrame
df = pd.read_csv(file_path)

# This is a dummy line of code so we have a place to pause
print("System paused. Ready to inspect the data.")

# STEP 1: Look at the margin to the left of line number 13
# STEP 2: Click the empty space to see a Red Dot appear
# STEP 3: This Red Dot is a 'Breakpoint'
done_message = "Data inspection complete."